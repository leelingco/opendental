//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 5:58:05 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;
import OpenDentBusiness.Db;
import OpenDentBusiness.DbHelper;
import OpenDentBusiness.Intervention;
import OpenDentBusiness.InterventionCodeSet;
import OpenDentBusiness.PIn;
import OpenDentBusiness.POut;
import OpenDentBusiness.PrefC;
import OpenDentBusiness.RemotingClient;
import OpenDentBusiness.RemotingRole;
import OpenDentBusiness.ReplicationServers;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class InterventionCrud   
{
    /**
    * Gets one Intervention object from the database using the primary key.  Returns null if not found.
    */
    public static Intervention selectOne(long interventionNum) throws Exception {
        String command = "SELECT * FROM intervention " + "WHERE InterventionNum = " + POut.long(interventionNum);
        List<Intervention> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one Intervention object from the database using a query.
    */
    public static Intervention selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Intervention> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of Intervention objects from the database using a query.
    */
    public static List<Intervention> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Intervention> list = tableToList(Db.getTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<Intervention> tableToList(DataTable table) throws Exception {
        List<Intervention> retVal = new List<Intervention>();
        Intervention intervention;
        for (int i = 0;i < table.Rows.Count;i++)
        {
            intervention = new Intervention();
            intervention.InterventionNum = PIn.Long(table.Rows[i]["InterventionNum"].ToString());
            intervention.PatNum = PIn.Long(table.Rows[i]["PatNum"].ToString());
            intervention.ProvNum = PIn.Long(table.Rows[i]["ProvNum"].ToString());
            intervention.CodeValue = PIn.String(table.Rows[i]["CodeValue"].ToString());
            intervention.CodeSystem = PIn.String(table.Rows[i]["CodeSystem"].ToString());
            intervention.Note = PIn.String(table.Rows[i]["Note"].ToString());
            intervention.DateEntry = PIn.Date(table.Rows[i]["DateEntry"].ToString());
            intervention.CodeSet = (InterventionCodeSet)PIn.Int(table.Rows[i]["CodeSet"].ToString());
            retVal.Add(intervention);
        }
        return retVal;
    }

    /**
    * Inserts one Intervention into the database.  Returns the new priKey.
    */
    public static long insert(Intervention intervention) throws Exception {
        if (OpenDentBusiness.DataConnection.DBtype == OpenDentBusiness.DatabaseType.Oracle)
        {
            intervention.InterventionNum = DbHelper.getNextOracleKey("intervention","InterventionNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return Insert(intervention, true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        intervention.InterventionNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return Insert(intervention, false);
        } 
    }

    /**
    * Inserts one Intervention into the database.  Provides option to use the existing priKey.
    */
    public static long insert(Intervention intervention, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.getRandomKeys())
        {
            intervention.InterventionNum = ReplicationServers.getKey("intervention","InterventionNum");
        }
         
        String command = "INSERT INTO intervention (";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += "InterventionNum,";
        }
         
        command += "PatNum,ProvNum,CodeValue,CodeSystem,Note,DateEntry,CodeSet) VALUES(";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += POut.long(intervention.InterventionNum) + ",";
        }
         
        command += POut.long(intervention.PatNum) + "," + POut.long(intervention.ProvNum) + "," + "'" + POut.string(intervention.CodeValue) + "'," + "'" + POut.string(intervention.CodeSystem) + "'," + "'" + POut.string(intervention.Note) + "'," + POut.date(intervention.DateEntry) + "," + POut.int(((Enum)intervention.CodeSet).ordinal()) + ")";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            Db.nonQ(command);
        }
        else
        {
            intervention.InterventionNum = Db.nonQ(command,true);
        } 
        return intervention.InterventionNum;
    }

    /**
    * Updates one Intervention in the database.
    */
    public static void update(Intervention intervention) throws Exception {
        String command = "UPDATE intervention SET " + "PatNum         =  " + POut.long(intervention.PatNum) + ", " + "ProvNum        =  " + POut.long(intervention.ProvNum) + ", " + "CodeValue      = '" + POut.string(intervention.CodeValue) + "', " + "CodeSystem     = '" + POut.string(intervention.CodeSystem) + "', " + "Note           = '" + POut.string(intervention.Note) + "', " + "DateEntry      =  " + POut.date(intervention.DateEntry) + ", " + "CodeSet        =  " + POut.int(((Enum)intervention.CodeSet).ordinal()) + " " + "WHERE InterventionNum = " + POut.long(intervention.InterventionNum);
        Db.nonQ(command);
    }

    /**
    * Updates one Intervention in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(Intervention intervention, Intervention oldIntervention) throws Exception {
        String command = "";
        if (intervention.PatNum != oldIntervention.PatNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "PatNum = " + POut.long(intervention.PatNum) + "";
        }
         
        if (intervention.ProvNum != oldIntervention.ProvNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "ProvNum = " + POut.long(intervention.ProvNum) + "";
        }
         
        if (!StringSupport.equals(intervention.CodeValue, oldIntervention.CodeValue))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CodeValue = '" + POut.string(intervention.CodeValue) + "'";
        }
         
        if (!StringSupport.equals(intervention.CodeSystem, oldIntervention.CodeSystem))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CodeSystem = '" + POut.string(intervention.CodeSystem) + "'";
        }
         
        if (!StringSupport.equals(intervention.Note, oldIntervention.Note))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Note = '" + POut.string(intervention.Note) + "'";
        }
         
        if (intervention.DateEntry != oldIntervention.DateEntry)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "DateEntry = " + POut.date(intervention.DateEntry) + "";
        }
         
        if (intervention.CodeSet != oldIntervention.CodeSet)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CodeSet = " + POut.int(((Enum)intervention.CodeSet).ordinal()) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE intervention SET " + command + " WHERE InterventionNum = " + POut.long(intervention.InterventionNum);
        Db.nonQ(command);
    }

    /**
    * Deletes one Intervention from the database.
    */
    public static void delete(long interventionNum) throws Exception {
        String command = "DELETE FROM intervention " + "WHERE InterventionNum = " + POut.long(interventionNum);
        Db.nonQ(command);
    }

}


