//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 5:58:06 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;
import OpenDentBusiness.Db;
import OpenDentBusiness.DbHelper;
import OpenDentBusiness.PatFieldDef;
import OpenDentBusiness.PatFieldType;
import OpenDentBusiness.PIn;
import OpenDentBusiness.POut;
import OpenDentBusiness.PrefC;
import OpenDentBusiness.RemotingClient;
import OpenDentBusiness.RemotingRole;
import OpenDentBusiness.ReplicationServers;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class PatFieldDefCrud   
{
    /**
    * Gets one PatFieldDef object from the database using the primary key.  Returns null if not found.
    */
    public static PatFieldDef selectOne(long patFieldDefNum) throws Exception {
        String command = "SELECT * FROM patfielddef " + "WHERE PatFieldDefNum = " + POut.long(patFieldDefNum);
        List<PatFieldDef> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one PatFieldDef object from the database using a query.
    */
    public static PatFieldDef selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<PatFieldDef> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of PatFieldDef objects from the database using a query.
    */
    public static List<PatFieldDef> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<PatFieldDef> list = tableToList(Db.getTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<PatFieldDef> tableToList(DataTable table) throws Exception {
        List<PatFieldDef> retVal = new List<PatFieldDef>();
        PatFieldDef patFieldDef;
        for (int i = 0;i < table.Rows.Count;i++)
        {
            patFieldDef = new PatFieldDef();
            patFieldDef.PatFieldDefNum = PIn.Long(table.Rows[i]["PatFieldDefNum"].ToString());
            patFieldDef.FieldName = PIn.String(table.Rows[i]["FieldName"].ToString());
            patFieldDef.FieldType = (PatFieldType)PIn.Int(table.Rows[i]["FieldType"].ToString());
            patFieldDef.PickList = PIn.String(table.Rows[i]["PickList"].ToString());
            retVal.Add(patFieldDef);
        }
        return retVal;
    }

    /**
    * Inserts one PatFieldDef into the database.  Returns the new priKey.
    */
    public static long insert(PatFieldDef patFieldDef) throws Exception {
        if (OpenDentBusiness.DataConnection.DBtype == OpenDentBusiness.DatabaseType.Oracle)
        {
            patFieldDef.PatFieldDefNum = DbHelper.getNextOracleKey("patfielddef","PatFieldDefNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return Insert(patFieldDef, true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        patFieldDef.PatFieldDefNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return Insert(patFieldDef, false);
        } 
    }

    /**
    * Inserts one PatFieldDef into the database.  Provides option to use the existing priKey.
    */
    public static long insert(PatFieldDef patFieldDef, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.getRandomKeys())
        {
            patFieldDef.PatFieldDefNum = ReplicationServers.getKey("patfielddef","PatFieldDefNum");
        }
         
        String command = "INSERT INTO patfielddef (";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += "PatFieldDefNum,";
        }
         
        command += "FieldName,FieldType,PickList) VALUES(";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += POut.long(patFieldDef.PatFieldDefNum) + ",";
        }
         
        command += "'" + POut.string(patFieldDef.FieldName) + "'," + POut.int(((Enum)patFieldDef.FieldType).ordinal()) + "," + "'" + POut.string(patFieldDef.PickList) + "')";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            Db.nonQ(command);
        }
        else
        {
            patFieldDef.PatFieldDefNum = Db.nonQ(command,true);
        } 
        return patFieldDef.PatFieldDefNum;
    }

    /**
    * Updates one PatFieldDef in the database.
    */
    public static void update(PatFieldDef patFieldDef) throws Exception {
        String command = "UPDATE patfielddef SET " + "FieldName     = '" + POut.string(patFieldDef.FieldName) + "', " + "FieldType     =  " + POut.int(((Enum)patFieldDef.FieldType).ordinal()) + ", " + "PickList      = '" + POut.string(patFieldDef.PickList) + "' " + "WHERE PatFieldDefNum = " + POut.long(patFieldDef.PatFieldDefNum);
        Db.nonQ(command);
    }

    /**
    * Updates one PatFieldDef in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(PatFieldDef patFieldDef, PatFieldDef oldPatFieldDef) throws Exception {
        String command = "";
        if (!StringSupport.equals(patFieldDef.FieldName, oldPatFieldDef.FieldName))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "FieldName = '" + POut.string(patFieldDef.FieldName) + "'";
        }
         
        if (patFieldDef.FieldType != oldPatFieldDef.FieldType)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "FieldType = " + POut.int(((Enum)patFieldDef.FieldType).ordinal()) + "";
        }
         
        if (!StringSupport.equals(patFieldDef.PickList, oldPatFieldDef.PickList))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "PickList = '" + POut.string(patFieldDef.PickList) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE patfielddef SET " + command + " WHERE PatFieldDefNum = " + POut.long(patFieldDef.PatFieldDefNum);
        Db.nonQ(command);
    }

    /**
    * Deletes one PatFieldDef from the database.
    */
    public static void delete(long patFieldDefNum) throws Exception {
        String command = "DELETE FROM patfielddef " + "WHERE PatFieldDefNum = " + POut.long(patFieldDefNum);
        Db.nonQ(command);
    }

}


