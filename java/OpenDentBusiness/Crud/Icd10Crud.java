//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 5:58:04 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;
import OpenDentBusiness.Db;
import OpenDentBusiness.DbHelper;
import OpenDentBusiness.Icd10;
import OpenDentBusiness.PIn;
import OpenDentBusiness.POut;
import OpenDentBusiness.PrefC;
import OpenDentBusiness.RemotingClient;
import OpenDentBusiness.RemotingRole;
import OpenDentBusiness.ReplicationServers;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class Icd10Crud   
{
    /**
    * Gets one Icd10 object from the database using the primary key.  Returns null if not found.
    */
    public static Icd10 selectOne(long icd10Num) throws Exception {
        String command = "SELECT * FROM icd10 " + "WHERE Icd10Num = " + POut.long(icd10Num);
        List<Icd10> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one Icd10 object from the database using a query.
    */
    public static Icd10 selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Icd10> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of Icd10 objects from the database using a query.
    */
    public static List<Icd10> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Icd10> list = tableToList(Db.getTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<Icd10> tableToList(DataTable table) throws Exception {
        List<Icd10> retVal = new List<Icd10>();
        Icd10 icd10;
        for (int i = 0;i < table.Rows.Count;i++)
        {
            icd10 = new Icd10();
            icd10.Icd10Num = PIn.Long(table.Rows[i]["Icd10Num"].ToString());
            icd10.Icd10Code = PIn.String(table.Rows[i]["Icd10Code"].ToString());
            icd10.Description = PIn.String(table.Rows[i]["Description"].ToString());
            icd10.IsCode = PIn.String(table.Rows[i]["IsCode"].ToString());
            retVal.Add(icd10);
        }
        return retVal;
    }

    /**
    * Inserts one Icd10 into the database.  Returns the new priKey.
    */
    public static long insert(Icd10 icd10) throws Exception {
        if (OpenDentBusiness.DataConnection.DBtype == OpenDentBusiness.DatabaseType.Oracle)
        {
            icd10.Icd10Num = DbHelper.getNextOracleKey("icd10","Icd10Num");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return Insert(icd10, true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        icd10.Icd10Num++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return Insert(icd10, false);
        } 
    }

    /**
    * Inserts one Icd10 into the database.  Provides option to use the existing priKey.
    */
    public static long insert(Icd10 icd10, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.getRandomKeys())
        {
            icd10.Icd10Num = ReplicationServers.getKey("icd10","Icd10Num");
        }
         
        String command = "INSERT INTO icd10 (";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += "Icd10Num,";
        }
         
        command += "Icd10Code,Description,IsCode) VALUES(";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += POut.long(icd10.Icd10Num) + ",";
        }
         
        command += "'" + POut.string(icd10.Icd10Code) + "'," + "'" + POut.string(icd10.Description) + "'," + "'" + POut.string(icd10.IsCode) + "')";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            Db.nonQ(command);
        }
        else
        {
            icd10.Icd10Num = Db.nonQ(command,true);
        } 
        return icd10.Icd10Num;
    }

    /**
    * Updates one Icd10 in the database.
    */
    public static void update(Icd10 icd10) throws Exception {
        String command = "UPDATE icd10 SET " + "Icd10Code  = '" + POut.string(icd10.Icd10Code) + "', " + "Description= '" + POut.string(icd10.Description) + "', " + "IsCode     = '" + POut.string(icd10.IsCode) + "' " + "WHERE Icd10Num = " + POut.long(icd10.Icd10Num);
        Db.nonQ(command);
    }

    /**
    * Updates one Icd10 in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(Icd10 icd10, Icd10 oldIcd10) throws Exception {
        String command = "";
        if (!StringSupport.equals(icd10.Icd10Code, oldIcd10.Icd10Code))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Icd10Code = '" + POut.string(icd10.Icd10Code) + "'";
        }
         
        if (!StringSupport.equals(icd10.Description, oldIcd10.Description))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.string(icd10.Description) + "'";
        }
         
        if (!StringSupport.equals(icd10.IsCode, oldIcd10.IsCode))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "IsCode = '" + POut.string(icd10.IsCode) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE icd10 SET " + command + " WHERE Icd10Num = " + POut.long(icd10.Icd10Num);
        Db.nonQ(command);
    }

    /**
    * Deletes one Icd10 from the database.
    */
    public static void delete(long icd10Num) throws Exception {
        String command = "DELETE FROM icd10 " + "WHERE Icd10Num = " + POut.long(icd10Num);
        Db.nonQ(command);
    }

}


