//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 5:57:58 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;
import OpenDentBusiness.Cdcrec;
import OpenDentBusiness.Db;
import OpenDentBusiness.DbHelper;
import OpenDentBusiness.PIn;
import OpenDentBusiness.POut;
import OpenDentBusiness.PrefC;
import OpenDentBusiness.RemotingClient;
import OpenDentBusiness.RemotingRole;
import OpenDentBusiness.ReplicationServers;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class CdcrecCrud   
{
    /**
    * Gets one Cdcrec object from the database using the primary key.  Returns null if not found.
    */
    public static Cdcrec selectOne(long cdcrecNum) throws Exception {
        String command = "SELECT * FROM cdcrec " + "WHERE CdcrecNum = " + POut.long(cdcrecNum);
        List<Cdcrec> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one Cdcrec object from the database using a query.
    */
    public static Cdcrec selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Cdcrec> list = tableToList(Db.getTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of Cdcrec objects from the database using a query.
    */
    public static List<Cdcrec> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Cdcrec> list = tableToList(Db.getTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<Cdcrec> tableToList(DataTable table) throws Exception {
        List<Cdcrec> retVal = new List<Cdcrec>();
        Cdcrec cdcrec;
        for (int i = 0;i < table.Rows.Count;i++)
        {
            cdcrec = new Cdcrec();
            cdcrec.CdcrecNum = PIn.Long(table.Rows[i]["CdcrecNum"].ToString());
            cdcrec.CdcrecCode = PIn.String(table.Rows[i]["CdcrecCode"].ToString());
            cdcrec.HeirarchicalCode = PIn.String(table.Rows[i]["HeirarchicalCode"].ToString());
            cdcrec.Description = PIn.String(table.Rows[i]["Description"].ToString());
            retVal.Add(cdcrec);
        }
        return retVal;
    }

    /**
    * Inserts one Cdcrec into the database.  Returns the new priKey.
    */
    public static long insert(Cdcrec cdcrec) throws Exception {
        if (OpenDentBusiness.DataConnection.DBtype == OpenDentBusiness.DatabaseType.Oracle)
        {
            cdcrec.CdcrecNum = DbHelper.getNextOracleKey("cdcrec","CdcrecNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return Insert(cdcrec, true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        cdcrec.CdcrecNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return Insert(cdcrec, false);
        } 
    }

    /**
    * Inserts one Cdcrec into the database.  Provides option to use the existing priKey.
    */
    public static long insert(Cdcrec cdcrec, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.getRandomKeys())
        {
            cdcrec.CdcrecNum = ReplicationServers.getKey("cdcrec","CdcrecNum");
        }
         
        String command = "INSERT INTO cdcrec (";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += "CdcrecNum,";
        }
         
        command += "CdcrecCode,HeirarchicalCode,Description) VALUES(";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            command += POut.long(cdcrec.CdcrecNum) + ",";
        }
         
        command += "'" + POut.string(cdcrec.CdcrecCode) + "'," + "'" + POut.string(cdcrec.HeirarchicalCode) + "'," + "'" + POut.string(cdcrec.Description) + "')";
        if (useExistingPK || PrefC.getRandomKeys())
        {
            Db.nonQ(command);
        }
        else
        {
            cdcrec.CdcrecNum = Db.nonQ(command,true);
        } 
        return cdcrec.CdcrecNum;
    }

    /**
    * Updates one Cdcrec in the database.
    */
    public static void update(Cdcrec cdcrec) throws Exception {
        String command = "UPDATE cdcrec SET " + "CdcrecCode      = '" + POut.string(cdcrec.CdcrecCode) + "', " + "HeirarchicalCode= '" + POut.string(cdcrec.HeirarchicalCode) + "', " + "Description     = '" + POut.string(cdcrec.Description) + "' " + "WHERE CdcrecNum = " + POut.long(cdcrec.CdcrecNum);
        Db.nonQ(command);
    }

    /**
    * Updates one Cdcrec in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(Cdcrec cdcrec, Cdcrec oldCdcrec) throws Exception {
        String command = "";
        if (!StringSupport.equals(cdcrec.CdcrecCode, oldCdcrec.CdcrecCode))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CdcrecCode = '" + POut.string(cdcrec.CdcrecCode) + "'";
        }
         
        if (!StringSupport.equals(cdcrec.HeirarchicalCode, oldCdcrec.HeirarchicalCode))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "HeirarchicalCode = '" + POut.string(cdcrec.HeirarchicalCode) + "'";
        }
         
        if (!StringSupport.equals(cdcrec.Description, oldCdcrec.Description))
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.string(cdcrec.Description) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE cdcrec SET " + command + " WHERE CdcrecNum = " + POut.long(cdcrec.CdcrecNum);
        Db.nonQ(command);
    }

    /**
    * Deletes one Cdcrec from the database.
    */
    public static void delete(long cdcrecNum) throws Exception {
        String command = "DELETE FROM cdcrec " + "WHERE CdcrecNum = " + POut.long(cdcrecNum);
        Db.nonQ(command);
    }

}


