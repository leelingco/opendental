//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:05 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class QuestionDefCrud   
{
    /**
    * Gets one QuestionDef object from the database using the primary key.  Returns null if not found.
    */
    public static QuestionDef selectOne(long questionDefNum) throws Exception {
        String command = "SELECT * FROM questiondef " + "WHERE QuestionDefNum = " + POut.Long(questionDefNum);
        List<QuestionDef> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one QuestionDef object from the database using a query.
    */
    public static QuestionDef selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<QuestionDef> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of QuestionDef objects from the database using a query.
    */
    public static List<QuestionDef> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<QuestionDef> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<QuestionDef> tableToList(DataTable table) throws Exception {
        List<QuestionDef> retVal = new List<QuestionDef>();
        QuestionDef questionDef = new QuestionDef();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            questionDef = new QuestionDef();
            questionDef.QuestionDefNum = PIn.Long(table.Rows[i]["QuestionDefNum"].ToString());
            questionDef.Description = PIn.String(table.Rows[i]["Description"].ToString());
            questionDef.ItemOrder = PIn.Int(table.Rows[i]["ItemOrder"].ToString());
            questionDef.QuestType = (QuestionType)PIn.Int(table.Rows[i]["QuestType"].ToString());
            retVal.Add(questionDef);
        }
        return retVal;
    }

    /**
    * Inserts one QuestionDef into the database.  Returns the new priKey.
    */
    public static long insert(QuestionDef questionDef) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            questionDef.QuestionDefNum = DbHelper.GetNextOracleKey("questiondef", "QuestionDefNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(questionDef,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        questionDef.QuestionDefNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(questionDef,false);
        } 
    }

    /**
    * Inserts one QuestionDef into the database.  Provides option to use the existing priKey.
    */
    public static long insert(QuestionDef questionDef, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            questionDef.QuestionDefNum = ReplicationServers.GetKey("questiondef", "QuestionDefNum");
        }
         
        String command = "INSERT INTO questiondef (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "QuestionDefNum,";
        }
         
        command += "Description,ItemOrder,QuestType) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(questionDef.QuestionDefNum) + ",";
        }
         
        command += "'" + POut.String(questionDef.Description) + "'," + POut.Int(questionDef.ItemOrder) + "," + POut.Int((int)questionDef.QuestType) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            questionDef.QuestionDefNum = Db.NonQ(command, true);
        } 
        return questionDef.QuestionDefNum;
    }

    /**
    * Updates one QuestionDef in the database.
    */
    public static void update(QuestionDef questionDef) throws Exception {
        String command = "UPDATE questiondef SET " + "Description   = '" + POut.String(questionDef.Description) + "', " + "ItemOrder     =  " + POut.Int(questionDef.ItemOrder) + ", " + "QuestType     =  " + POut.Int((int)questionDef.QuestType) + " " + "WHERE QuestionDefNum = " + POut.Long(questionDef.QuestionDefNum);
        Db.NonQ(command);
    }

    /**
    * Updates one QuestionDef in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(QuestionDef questionDef, QuestionDef oldQuestionDef) throws Exception {
        String command = "";
        if (questionDef.Description != oldQuestionDef.Description)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.String(questionDef.Description) + "'";
        }
         
        if (questionDef.ItemOrder != oldQuestionDef.ItemOrder)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "ItemOrder = " + POut.Int(questionDef.ItemOrder) + "";
        }
         
        if (questionDef.QuestType != oldQuestionDef.QuestType)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "QuestType = " + POut.Int((int)questionDef.QuestType) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE questiondef SET " + command + " WHERE QuestionDefNum = " + POut.Long(questionDef.QuestionDefNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one QuestionDef from the database.
    */
    public static void delete(long questionDefNum) throws Exception {
        String command = "DELETE FROM questiondef " + "WHERE QuestionDefNum = " + POut.Long(questionDefNum);
        Db.NonQ(command);
    }

}


