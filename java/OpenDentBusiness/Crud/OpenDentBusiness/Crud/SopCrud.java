//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:07 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class SopCrud   
{
    /**
    * Gets one Sop object from the database using the primary key.  Returns null if not found.
    */
    public static Sop selectOne(long sopNum) throws Exception {
        String command = "SELECT * FROM sop " + "WHERE SopNum = " + POut.Long(sopNum);
        List<Sop> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one Sop object from the database using a query.
    */
    public static Sop selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Sop> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of Sop objects from the database using a query.
    */
    public static List<Sop> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Sop> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<Sop> tableToList(DataTable table) throws Exception {
        List<Sop> retVal = new List<Sop>();
        Sop sop = new Sop();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            sop = new Sop();
            sop.SopNum = PIn.Long(table.Rows[i]["SopNum"].ToString());
            sop.SopCode = PIn.String(table.Rows[i]["SopCode"].ToString());
            sop.Description = PIn.String(table.Rows[i]["Description"].ToString());
            retVal.Add(sop);
        }
        return retVal;
    }

    /**
    * Inserts one Sop into the database.  Returns the new priKey.
    */
    public static long insert(Sop sop) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            sop.SopNum = DbHelper.GetNextOracleKey("sop", "SopNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(sop,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        sop.SopNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(sop,false);
        } 
    }

    /**
    * Inserts one Sop into the database.  Provides option to use the existing priKey.
    */
    public static long insert(Sop sop, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            sop.SopNum = ReplicationServers.GetKey("sop", "SopNum");
        }
         
        String command = "INSERT INTO sop (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "SopNum,";
        }
         
        command += "SopCode,Description) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(sop.SopNum) + ",";
        }
         
        command += "'" + POut.String(sop.SopCode) + "'," + "'" + POut.String(sop.Description) + "')";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            sop.SopNum = Db.NonQ(command, true);
        } 
        return sop.SopNum;
    }

    /**
    * Updates one Sop in the database.
    */
    public static void update(Sop sop) throws Exception {
        String command = "UPDATE sop SET " + "SopCode    = '" + POut.String(sop.SopCode) + "', " + "Description= '" + POut.String(sop.Description) + "' " + "WHERE SopNum = " + POut.Long(sop.SopNum);
        Db.NonQ(command);
    }

    /**
    * Updates one Sop in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(Sop sop, Sop oldSop) throws Exception {
        String command = "";
        if (sop.SopCode != oldSop.SopCode)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "SopCode = '" + POut.String(sop.SopCode) + "'";
        }
         
        if (sop.Description != oldSop.Description)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.String(sop.Description) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE sop SET " + command + " WHERE SopNum = " + POut.Long(sop.SopNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one Sop from the database.
    */
    public static void delete(long sopNum) throws Exception {
        String command = "DELETE FROM sop " + "WHERE SopNum = " + POut.Long(sopNum);
        Db.NonQ(command);
    }

}


