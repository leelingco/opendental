//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:08 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class TaskAncestorCrud   
{
    /**
    * Gets one TaskAncestor object from the database using the primary key.  Returns null if not found.
    */
    public static TaskAncestor selectOne(long taskAncestorNum) throws Exception {
        String command = "SELECT * FROM taskancestor " + "WHERE TaskAncestorNum = " + POut.Long(taskAncestorNum);
        List<TaskAncestor> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one TaskAncestor object from the database using a query.
    */
    public static TaskAncestor selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<TaskAncestor> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of TaskAncestor objects from the database using a query.
    */
    public static List<TaskAncestor> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<TaskAncestor> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<TaskAncestor> tableToList(DataTable table) throws Exception {
        List<TaskAncestor> retVal = new List<TaskAncestor>();
        TaskAncestor taskAncestor = new TaskAncestor();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            taskAncestor = new TaskAncestor();
            taskAncestor.TaskAncestorNum = PIn.Long(table.Rows[i]["TaskAncestorNum"].ToString());
            taskAncestor.TaskNum = PIn.Long(table.Rows[i]["TaskNum"].ToString());
            taskAncestor.TaskListNum = PIn.Long(table.Rows[i]["TaskListNum"].ToString());
            retVal.Add(taskAncestor);
        }
        return retVal;
    }

    /**
    * Inserts one TaskAncestor into the database.  Returns the new priKey.
    */
    public static long insert(TaskAncestor taskAncestor) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            taskAncestor.TaskAncestorNum = DbHelper.GetNextOracleKey("taskancestor", "TaskAncestorNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(taskAncestor,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        taskAncestor.TaskAncestorNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(taskAncestor,false);
        } 
    }

    /**
    * Inserts one TaskAncestor into the database.  Provides option to use the existing priKey.
    */
    public static long insert(TaskAncestor taskAncestor, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            taskAncestor.TaskAncestorNum = ReplicationServers.GetKey("taskancestor", "TaskAncestorNum");
        }
         
        String command = "INSERT INTO taskancestor (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "TaskAncestorNum,";
        }
         
        command += "TaskNum,TaskListNum) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(taskAncestor.TaskAncestorNum) + ",";
        }
         
        command += POut.Long(taskAncestor.TaskNum) + "," + POut.Long(taskAncestor.TaskListNum) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            taskAncestor.TaskAncestorNum = Db.NonQ(command, true);
        } 
        return taskAncestor.TaskAncestorNum;
    }

    /**
    * Updates one TaskAncestor in the database.
    */
    public static void update(TaskAncestor taskAncestor) throws Exception {
        String command = "UPDATE taskancestor SET " + "TaskNum        =  " + POut.Long(taskAncestor.TaskNum) + ", " + "TaskListNum    =  " + POut.Long(taskAncestor.TaskListNum) + " " + "WHERE TaskAncestorNum = " + POut.Long(taskAncestor.TaskAncestorNum);
        Db.NonQ(command);
    }

    /**
    * Updates one TaskAncestor in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(TaskAncestor taskAncestor, TaskAncestor oldTaskAncestor) throws Exception {
        String command = "";
        if (taskAncestor.TaskNum != oldTaskAncestor.TaskNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "TaskNum = " + POut.Long(taskAncestor.TaskNum) + "";
        }
         
        if (taskAncestor.TaskListNum != oldTaskAncestor.TaskListNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "TaskListNum = " + POut.Long(taskAncestor.TaskListNum) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE taskancestor SET " + command + " WHERE TaskAncestorNum = " + POut.Long(taskAncestor.TaskAncestorNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one TaskAncestor from the database.
    */
    public static void delete(long taskAncestorNum) throws Exception {
        String command = "DELETE FROM taskancestor " + "WHERE TaskAncestorNum = " + POut.Long(taskAncestorNum);
        Db.NonQ(command);
    }

}


