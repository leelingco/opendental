//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:08 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class TaskUnreadCrud   
{
    /**
    * Gets one TaskUnread object from the database using the primary key.  Returns null if not found.
    */
    public static TaskUnread selectOne(long taskUnreadNum) throws Exception {
        String command = "SELECT * FROM taskunread " + "WHERE TaskUnreadNum = " + POut.Long(taskUnreadNum);
        List<TaskUnread> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one TaskUnread object from the database using a query.
    */
    public static TaskUnread selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<TaskUnread> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of TaskUnread objects from the database using a query.
    */
    public static List<TaskUnread> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<TaskUnread> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<TaskUnread> tableToList(DataTable table) throws Exception {
        List<TaskUnread> retVal = new List<TaskUnread>();
        TaskUnread taskUnread = new TaskUnread();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            taskUnread = new TaskUnread();
            taskUnread.TaskUnreadNum = PIn.Long(table.Rows[i]["TaskUnreadNum"].ToString());
            taskUnread.TaskNum = PIn.Long(table.Rows[i]["TaskNum"].ToString());
            taskUnread.UserNum = PIn.Long(table.Rows[i]["UserNum"].ToString());
            retVal.Add(taskUnread);
        }
        return retVal;
    }

    /**
    * Inserts one TaskUnread into the database.  Returns the new priKey.
    */
    public static long insert(TaskUnread taskUnread) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            taskUnread.TaskUnreadNum = DbHelper.GetNextOracleKey("taskunread", "TaskUnreadNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(taskUnread,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        taskUnread.TaskUnreadNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(taskUnread,false);
        } 
    }

    /**
    * Inserts one TaskUnread into the database.  Provides option to use the existing priKey.
    */
    public static long insert(TaskUnread taskUnread, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            taskUnread.TaskUnreadNum = ReplicationServers.GetKey("taskunread", "TaskUnreadNum");
        }
         
        String command = "INSERT INTO taskunread (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "TaskUnreadNum,";
        }
         
        command += "TaskNum,UserNum) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(taskUnread.TaskUnreadNum) + ",";
        }
         
        command += POut.Long(taskUnread.TaskNum) + "," + POut.Long(taskUnread.UserNum) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            taskUnread.TaskUnreadNum = Db.NonQ(command, true);
        } 
        return taskUnread.TaskUnreadNum;
    }

    /**
    * Updates one TaskUnread in the database.
    */
    public static void update(TaskUnread taskUnread) throws Exception {
        String command = "UPDATE taskunread SET " + "TaskNum      =  " + POut.Long(taskUnread.TaskNum) + ", " + "UserNum      =  " + POut.Long(taskUnread.UserNum) + " " + "WHERE TaskUnreadNum = " + POut.Long(taskUnread.TaskUnreadNum);
        Db.NonQ(command);
    }

    /**
    * Updates one TaskUnread in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(TaskUnread taskUnread, TaskUnread oldTaskUnread) throws Exception {
        String command = "";
        if (taskUnread.TaskNum != oldTaskUnread.TaskNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "TaskNum = " + POut.Long(taskUnread.TaskNum) + "";
        }
         
        if (taskUnread.UserNum != oldTaskUnread.UserNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "UserNum = " + POut.Long(taskUnread.UserNum) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE taskunread SET " + command + " WHERE TaskUnreadNum = " + POut.Long(taskUnread.TaskUnreadNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one TaskUnread from the database.
    */
    public static void delete(long taskUnreadNum) throws Exception {
        String command = "DELETE FROM taskunread " + "WHERE TaskUnreadNum = " + POut.Long(taskUnreadNum);
        Db.NonQ(command);
    }

}


