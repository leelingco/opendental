//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:06 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class ReqNeededCrud   
{
    /**
    * Gets one ReqNeeded object from the database using the primary key.  Returns null if not found.
    */
    public static ReqNeeded selectOne(long reqNeededNum) throws Exception {
        String command = "SELECT * FROM reqneeded " + "WHERE ReqNeededNum = " + POut.Long(reqNeededNum);
        List<ReqNeeded> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one ReqNeeded object from the database using a query.
    */
    public static ReqNeeded selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<ReqNeeded> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of ReqNeeded objects from the database using a query.
    */
    public static List<ReqNeeded> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<ReqNeeded> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<ReqNeeded> tableToList(DataTable table) throws Exception {
        List<ReqNeeded> retVal = new List<ReqNeeded>();
        ReqNeeded reqNeeded = new ReqNeeded();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            reqNeeded = new ReqNeeded();
            reqNeeded.ReqNeededNum = PIn.Long(table.Rows[i]["ReqNeededNum"].ToString());
            reqNeeded.Descript = PIn.String(table.Rows[i]["Descript"].ToString());
            reqNeeded.SchoolCourseNum = PIn.Long(table.Rows[i]["SchoolCourseNum"].ToString());
            reqNeeded.SchoolClassNum = PIn.Long(table.Rows[i]["SchoolClassNum"].ToString());
            retVal.Add(reqNeeded);
        }
        return retVal;
    }

    /**
    * Inserts one ReqNeeded into the database.  Returns the new priKey.
    */
    public static long insert(ReqNeeded reqNeeded) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            reqNeeded.ReqNeededNum = DbHelper.GetNextOracleKey("reqneeded", "ReqNeededNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(reqNeeded,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        reqNeeded.ReqNeededNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(reqNeeded,false);
        } 
    }

    /**
    * Inserts one ReqNeeded into the database.  Provides option to use the existing priKey.
    */
    public static long insert(ReqNeeded reqNeeded, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            reqNeeded.ReqNeededNum = ReplicationServers.GetKey("reqneeded", "ReqNeededNum");
        }
         
        String command = "INSERT INTO reqneeded (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "ReqNeededNum,";
        }
         
        command += "Descript,SchoolCourseNum,SchoolClassNum) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(reqNeeded.ReqNeededNum) + ",";
        }
         
        command += "'" + POut.String(reqNeeded.Descript) + "'," + POut.Long(reqNeeded.SchoolCourseNum) + "," + POut.Long(reqNeeded.SchoolClassNum) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            reqNeeded.ReqNeededNum = Db.NonQ(command, true);
        } 
        return reqNeeded.ReqNeededNum;
    }

    /**
    * Updates one ReqNeeded in the database.
    */
    public static void update(ReqNeeded reqNeeded) throws Exception {
        String command = "UPDATE reqneeded SET " + "Descript       = '" + POut.String(reqNeeded.Descript) + "', " + "SchoolCourseNum=  " + POut.Long(reqNeeded.SchoolCourseNum) + ", " + "SchoolClassNum =  " + POut.Long(reqNeeded.SchoolClassNum) + " " + "WHERE ReqNeededNum = " + POut.Long(reqNeeded.ReqNeededNum);
        Db.NonQ(command);
    }

    /**
    * Updates one ReqNeeded in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(ReqNeeded reqNeeded, ReqNeeded oldReqNeeded) throws Exception {
        String command = "";
        if (reqNeeded.Descript != oldReqNeeded.Descript)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Descript = '" + POut.String(reqNeeded.Descript) + "'";
        }
         
        if (reqNeeded.SchoolCourseNum != oldReqNeeded.SchoolCourseNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "SchoolCourseNum = " + POut.Long(reqNeeded.SchoolCourseNum) + "";
        }
         
        if (reqNeeded.SchoolClassNum != oldReqNeeded.SchoolClassNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "SchoolClassNum = " + POut.Long(reqNeeded.SchoolClassNum) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE reqneeded SET " + command + " WHERE ReqNeededNum = " + POut.Long(reqNeeded.ReqNeededNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one ReqNeeded from the database.
    */
    public static void delete(long reqNeededNum) throws Exception {
        String command = "DELETE FROM reqneeded " + "WHERE ReqNeededNum = " + POut.Long(reqNeededNum);
        Db.NonQ(command);
    }

}


