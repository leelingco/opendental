//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:05 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class RecallTypeCrud   
{
    /**
    * Gets one RecallType object from the database using the primary key.  Returns null if not found.
    */
    public static RecallType selectOne(long recallTypeNum) throws Exception {
        String command = "SELECT * FROM recalltype " + "WHERE RecallTypeNum = " + POut.Long(recallTypeNum);
        List<RecallType> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one RecallType object from the database using a query.
    */
    public static RecallType selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<RecallType> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of RecallType objects from the database using a query.
    */
    public static List<RecallType> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<RecallType> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<RecallType> tableToList(DataTable table) throws Exception {
        List<RecallType> retVal = new List<RecallType>();
        RecallType recallType = new RecallType();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            recallType = new RecallType();
            recallType.RecallTypeNum = PIn.Long(table.Rows[i]["RecallTypeNum"].ToString());
            recallType.Description = PIn.String(table.Rows[i]["Description"].ToString());
            recallType.DefaultInterval = new Interval(PIn.Int(table.Rows[i]["DefaultInterval"].ToString()));
            recallType.TimePattern = PIn.String(table.Rows[i]["TimePattern"].ToString());
            recallType.Procedures = PIn.String(table.Rows[i]["Procedures"].ToString());
            retVal.Add(recallType);
        }
        return retVal;
    }

    /**
    * Inserts one RecallType into the database.  Returns the new priKey.
    */
    public static long insert(RecallType recallType) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            recallType.RecallTypeNum = DbHelper.GetNextOracleKey("recalltype", "RecallTypeNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(recallType,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        recallType.RecallTypeNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(recallType,false);
        } 
    }

    /**
    * Inserts one RecallType into the database.  Provides option to use the existing priKey.
    */
    public static long insert(RecallType recallType, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            recallType.RecallTypeNum = ReplicationServers.GetKey("recalltype", "RecallTypeNum");
        }
         
        String command = "INSERT INTO recalltype (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "RecallTypeNum,";
        }
         
        command += "Description,DefaultInterval,TimePattern,Procedures) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(recallType.RecallTypeNum) + ",";
        }
         
        command += "'" + POut.String(recallType.Description) + "'," + POut.Int(recallType.DefaultInterval.ToInt()) + "," + "'" + POut.String(recallType.TimePattern) + "'," + "'" + POut.String(recallType.Procedures) + "')";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            recallType.RecallTypeNum = Db.NonQ(command, true);
        } 
        return recallType.RecallTypeNum;
    }

    /**
    * Updates one RecallType in the database.
    */
    public static void update(RecallType recallType) throws Exception {
        String command = "UPDATE recalltype SET " + "Description    = '" + POut.String(recallType.Description) + "', " + "DefaultInterval=  " + POut.Int(recallType.DefaultInterval.ToInt()) + ", " + "TimePattern    = '" + POut.String(recallType.TimePattern) + "', " + "Procedures     = '" + POut.String(recallType.Procedures) + "' " + "WHERE RecallTypeNum = " + POut.Long(recallType.RecallTypeNum);
        Db.NonQ(command);
    }

    /**
    * Updates one RecallType in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(RecallType recallType, RecallType oldRecallType) throws Exception {
        String command = "";
        if (recallType.Description != oldRecallType.Description)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.String(recallType.Description) + "'";
        }
         
        if (recallType.DefaultInterval != oldRecallType.DefaultInterval)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "DefaultInterval = " + POut.Int(recallType.DefaultInterval.ToInt()) + "";
        }
         
        if (recallType.TimePattern != oldRecallType.TimePattern)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "TimePattern = '" + POut.String(recallType.TimePattern) + "'";
        }
         
        if (recallType.Procedures != oldRecallType.Procedures)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Procedures = '" + POut.String(recallType.Procedures) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE recalltype SET " + command + " WHERE RecallTypeNum = " + POut.Long(recallType.RecallTypeNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one RecallType from the database.
    */
    public static void delete(long recallTypeNum) throws Exception {
        String command = "DELETE FROM recalltype " + "WHERE RecallTypeNum = " + POut.Long(recallTypeNum);
        Db.NonQ(command);
    }

}


