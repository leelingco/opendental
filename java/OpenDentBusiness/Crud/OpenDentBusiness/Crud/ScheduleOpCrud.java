//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:06 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class ScheduleOpCrud   
{
    /**
    * Gets one ScheduleOp object from the database using the primary key.  Returns null if not found.
    */
    public static ScheduleOp selectOne(long scheduleOpNum) throws Exception {
        String command = "SELECT * FROM scheduleop " + "WHERE ScheduleOpNum = " + POut.Long(scheduleOpNum);
        List<ScheduleOp> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one ScheduleOp object from the database using a query.
    */
    public static ScheduleOp selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<ScheduleOp> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of ScheduleOp objects from the database using a query.
    */
    public static List<ScheduleOp> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<ScheduleOp> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<ScheduleOp> tableToList(DataTable table) throws Exception {
        List<ScheduleOp> retVal = new List<ScheduleOp>();
        ScheduleOp scheduleOp = new ScheduleOp();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            scheduleOp = new ScheduleOp();
            scheduleOp.ScheduleOpNum = PIn.Long(table.Rows[i]["ScheduleOpNum"].ToString());
            scheduleOp.ScheduleNum = PIn.Long(table.Rows[i]["ScheduleNum"].ToString());
            scheduleOp.OperatoryNum = PIn.Long(table.Rows[i]["OperatoryNum"].ToString());
            retVal.Add(scheduleOp);
        }
        return retVal;
    }

    /**
    * Inserts one ScheduleOp into the database.  Returns the new priKey.
    */
    public static long insert(ScheduleOp scheduleOp) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            scheduleOp.ScheduleOpNum = DbHelper.GetNextOracleKey("scheduleop", "ScheduleOpNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(scheduleOp,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        scheduleOp.ScheduleOpNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(scheduleOp,false);
        } 
    }

    /**
    * Inserts one ScheduleOp into the database.  Provides option to use the existing priKey.
    */
    public static long insert(ScheduleOp scheduleOp, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            scheduleOp.ScheduleOpNum = ReplicationServers.GetKey("scheduleop", "ScheduleOpNum");
        }
         
        String command = "INSERT INTO scheduleop (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "ScheduleOpNum,";
        }
         
        command += "ScheduleNum,OperatoryNum) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(scheduleOp.ScheduleOpNum) + ",";
        }
         
        command += POut.Long(scheduleOp.ScheduleNum) + "," + POut.Long(scheduleOp.OperatoryNum) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            scheduleOp.ScheduleOpNum = Db.NonQ(command, true);
        } 
        return scheduleOp.ScheduleOpNum;
    }

    /**
    * Updates one ScheduleOp in the database.
    */
    public static void update(ScheduleOp scheduleOp) throws Exception {
        String command = "UPDATE scheduleop SET " + "ScheduleNum  =  " + POut.Long(scheduleOp.ScheduleNum) + ", " + "OperatoryNum =  " + POut.Long(scheduleOp.OperatoryNum) + " " + "WHERE ScheduleOpNum = " + POut.Long(scheduleOp.ScheduleOpNum);
        Db.NonQ(command);
    }

    /**
    * Updates one ScheduleOp in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(ScheduleOp scheduleOp, ScheduleOp oldScheduleOp) throws Exception {
        String command = "";
        if (scheduleOp.ScheduleNum != oldScheduleOp.ScheduleNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "ScheduleNum = " + POut.Long(scheduleOp.ScheduleNum) + "";
        }
         
        if (scheduleOp.OperatoryNum != oldScheduleOp.OperatoryNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "OperatoryNum = " + POut.Long(scheduleOp.OperatoryNum) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE scheduleop SET " + command + " WHERE ScheduleOpNum = " + POut.Long(scheduleOp.ScheduleOpNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one ScheduleOp from the database.
    */
    public static void delete(long scheduleOpNum) throws Exception {
        String command = "DELETE FROM scheduleop " + "WHERE ScheduleOpNum = " + POut.Long(scheduleOpNum);
        Db.NonQ(command);
    }

}


