//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:06 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class SchoolCourseCrud   
{
    /**
    * Gets one SchoolCourse object from the database using the primary key.  Returns null if not found.
    */
    public static SchoolCourse selectOne(long schoolCourseNum) throws Exception {
        String command = "SELECT * FROM schoolcourse " + "WHERE SchoolCourseNum = " + POut.Long(schoolCourseNum);
        List<SchoolCourse> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one SchoolCourse object from the database using a query.
    */
    public static SchoolCourse selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<SchoolCourse> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of SchoolCourse objects from the database using a query.
    */
    public static List<SchoolCourse> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<SchoolCourse> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<SchoolCourse> tableToList(DataTable table) throws Exception {
        List<SchoolCourse> retVal = new List<SchoolCourse>();
        SchoolCourse schoolCourse = new SchoolCourse();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            schoolCourse = new SchoolCourse();
            schoolCourse.SchoolCourseNum = PIn.Long(table.Rows[i]["SchoolCourseNum"].ToString());
            schoolCourse.CourseID = PIn.String(table.Rows[i]["CourseID"].ToString());
            schoolCourse.Descript = PIn.String(table.Rows[i]["Descript"].ToString());
            retVal.Add(schoolCourse);
        }
        return retVal;
    }

    /**
    * Inserts one SchoolCourse into the database.  Returns the new priKey.
    */
    public static long insert(SchoolCourse schoolCourse) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            schoolCourse.SchoolCourseNum = DbHelper.GetNextOracleKey("schoolcourse", "SchoolCourseNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(schoolCourse,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        schoolCourse.SchoolCourseNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(schoolCourse,false);
        } 
    }

    /**
    * Inserts one SchoolCourse into the database.  Provides option to use the existing priKey.
    */
    public static long insert(SchoolCourse schoolCourse, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            schoolCourse.SchoolCourseNum = ReplicationServers.GetKey("schoolcourse", "SchoolCourseNum");
        }
         
        String command = "INSERT INTO schoolcourse (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "SchoolCourseNum,";
        }
         
        command += "CourseID,Descript) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(schoolCourse.SchoolCourseNum) + ",";
        }
         
        command += "'" + POut.String(schoolCourse.CourseID) + "'," + "'" + POut.String(schoolCourse.Descript) + "')";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            schoolCourse.SchoolCourseNum = Db.NonQ(command, true);
        } 
        return schoolCourse.SchoolCourseNum;
    }

    /**
    * Updates one SchoolCourse in the database.
    */
    public static void update(SchoolCourse schoolCourse) throws Exception {
        String command = "UPDATE schoolcourse SET " + "CourseID       = '" + POut.String(schoolCourse.CourseID) + "', " + "Descript       = '" + POut.String(schoolCourse.Descript) + "' " + "WHERE SchoolCourseNum = " + POut.Long(schoolCourse.SchoolCourseNum);
        Db.NonQ(command);
    }

    /**
    * Updates one SchoolCourse in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(SchoolCourse schoolCourse, SchoolCourse oldSchoolCourse) throws Exception {
        String command = "";
        if (schoolCourse.CourseID != oldSchoolCourse.CourseID)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CourseID = '" + POut.String(schoolCourse.CourseID) + "'";
        }
         
        if (schoolCourse.Descript != oldSchoolCourse.Descript)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Descript = '" + POut.String(schoolCourse.Descript) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE schoolcourse SET " + command + " WHERE SchoolCourseNum = " + POut.Long(schoolCourse.SchoolCourseNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one SchoolCourse from the database.
    */
    public static void delete(long schoolCourseNum) throws Exception {
        String command = "DELETE FROM schoolcourse " + "WHERE SchoolCourseNum = " + POut.Long(schoolCourseNum);
        Db.NonQ(command);
    }

}


