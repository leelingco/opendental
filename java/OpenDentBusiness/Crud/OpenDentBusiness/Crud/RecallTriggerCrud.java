//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:05 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class RecallTriggerCrud   
{
    /**
    * Gets one RecallTrigger object from the database using the primary key.  Returns null if not found.
    */
    public static RecallTrigger selectOne(long recallTriggerNum) throws Exception {
        String command = "SELECT * FROM recalltrigger " + "WHERE RecallTriggerNum = " + POut.Long(recallTriggerNum);
        List<RecallTrigger> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one RecallTrigger object from the database using a query.
    */
    public static RecallTrigger selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<RecallTrigger> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of RecallTrigger objects from the database using a query.
    */
    public static List<RecallTrigger> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<RecallTrigger> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<RecallTrigger> tableToList(DataTable table) throws Exception {
        List<RecallTrigger> retVal = new List<RecallTrigger>();
        RecallTrigger recallTrigger = new RecallTrigger();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            recallTrigger = new RecallTrigger();
            recallTrigger.RecallTriggerNum = PIn.Long(table.Rows[i]["RecallTriggerNum"].ToString());
            recallTrigger.RecallTypeNum = PIn.Long(table.Rows[i]["RecallTypeNum"].ToString());
            recallTrigger.CodeNum = PIn.Long(table.Rows[i]["CodeNum"].ToString());
            retVal.Add(recallTrigger);
        }
        return retVal;
    }

    /**
    * Inserts one RecallTrigger into the database.  Returns the new priKey.
    */
    public static long insert(RecallTrigger recallTrigger) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            recallTrigger.RecallTriggerNum = DbHelper.GetNextOracleKey("recalltrigger", "RecallTriggerNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(recallTrigger,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        recallTrigger.RecallTriggerNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(recallTrigger,false);
        } 
    }

    /**
    * Inserts one RecallTrigger into the database.  Provides option to use the existing priKey.
    */
    public static long insert(RecallTrigger recallTrigger, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            recallTrigger.RecallTriggerNum = ReplicationServers.GetKey("recalltrigger", "RecallTriggerNum");
        }
         
        String command = "INSERT INTO recalltrigger (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "RecallTriggerNum,";
        }
         
        command += "RecallTypeNum,CodeNum) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(recallTrigger.RecallTriggerNum) + ",";
        }
         
        command += POut.Long(recallTrigger.RecallTypeNum) + "," + POut.Long(recallTrigger.CodeNum) + ")";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            recallTrigger.RecallTriggerNum = Db.NonQ(command, true);
        } 
        return recallTrigger.RecallTriggerNum;
    }

    /**
    * Updates one RecallTrigger in the database.
    */
    public static void update(RecallTrigger recallTrigger) throws Exception {
        String command = "UPDATE recalltrigger SET " + "RecallTypeNum   =  " + POut.Long(recallTrigger.RecallTypeNum) + ", " + "CodeNum         =  " + POut.Long(recallTrigger.CodeNum) + " " + "WHERE RecallTriggerNum = " + POut.Long(recallTrigger.RecallTriggerNum);
        Db.NonQ(command);
    }

    /**
    * Updates one RecallTrigger in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(RecallTrigger recallTrigger, RecallTrigger oldRecallTrigger) throws Exception {
        String command = "";
        if (recallTrigger.RecallTypeNum != oldRecallTrigger.RecallTypeNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "RecallTypeNum = " + POut.Long(recallTrigger.RecallTypeNum) + "";
        }
         
        if (recallTrigger.CodeNum != oldRecallTrigger.CodeNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "CodeNum = " + POut.Long(recallTrigger.CodeNum) + "";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE recalltrigger SET " + command + " WHERE RecallTriggerNum = " + POut.Long(recallTrigger.RecallTriggerNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one RecallTrigger from the database.
    */
    public static void delete(long recallTriggerNum) throws Exception {
        String command = "DELETE FROM recalltrigger " + "WHERE RecallTriggerNum = " + POut.Long(recallTriggerNum);
        Db.NonQ(command);
    }

}


