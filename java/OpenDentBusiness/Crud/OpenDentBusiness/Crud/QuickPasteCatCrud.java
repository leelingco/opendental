//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:05 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class QuickPasteCatCrud   
{
    /**
    * Gets one QuickPasteCat object from the database using the primary key.  Returns null if not found.
    */
    public static QuickPasteCat selectOne(long quickPasteCatNum) throws Exception {
        String command = "SELECT * FROM quickpastecat " + "WHERE QuickPasteCatNum = " + POut.Long(quickPasteCatNum);
        List<QuickPasteCat> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one QuickPasteCat object from the database using a query.
    */
    public static QuickPasteCat selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<QuickPasteCat> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of QuickPasteCat objects from the database using a query.
    */
    public static List<QuickPasteCat> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<QuickPasteCat> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<QuickPasteCat> tableToList(DataTable table) throws Exception {
        List<QuickPasteCat> retVal = new List<QuickPasteCat>();
        QuickPasteCat quickPasteCat = new QuickPasteCat();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            quickPasteCat = new QuickPasteCat();
            quickPasteCat.QuickPasteCatNum = PIn.Long(table.Rows[i]["QuickPasteCatNum"].ToString());
            quickPasteCat.Description = PIn.String(table.Rows[i]["Description"].ToString());
            quickPasteCat.ItemOrder = PIn.Int(table.Rows[i]["ItemOrder"].ToString());
            quickPasteCat.DefaultForTypes = PIn.String(table.Rows[i]["DefaultForTypes"].ToString());
            retVal.Add(quickPasteCat);
        }
        return retVal;
    }

    /**
    * Inserts one QuickPasteCat into the database.  Returns the new priKey.
    */
    public static long insert(QuickPasteCat quickPasteCat) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            quickPasteCat.QuickPasteCatNum = DbHelper.GetNextOracleKey("quickpastecat", "QuickPasteCatNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(quickPasteCat,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        quickPasteCat.QuickPasteCatNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(quickPasteCat,false);
        } 
    }

    /**
    * Inserts one QuickPasteCat into the database.  Provides option to use the existing priKey.
    */
    public static long insert(QuickPasteCat quickPasteCat, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            quickPasteCat.QuickPasteCatNum = ReplicationServers.GetKey("quickpastecat", "QuickPasteCatNum");
        }
         
        String command = "INSERT INTO quickpastecat (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "QuickPasteCatNum,";
        }
         
        command += "Description,ItemOrder,DefaultForTypes) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(quickPasteCat.QuickPasteCatNum) + ",";
        }
         
        command += "'" + POut.String(quickPasteCat.Description) + "'," + POut.Int(quickPasteCat.ItemOrder) + "," + "'" + POut.String(quickPasteCat.DefaultForTypes) + "')";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            quickPasteCat.QuickPasteCatNum = Db.NonQ(command, true);
        } 
        return quickPasteCat.QuickPasteCatNum;
    }

    /**
    * Updates one QuickPasteCat in the database.
    */
    public static void update(QuickPasteCat quickPasteCat) throws Exception {
        String command = "UPDATE quickpastecat SET " + "Description     = '" + POut.String(quickPasteCat.Description) + "', " + "ItemOrder       =  " + POut.Int(quickPasteCat.ItemOrder) + ", " + "DefaultForTypes = '" + POut.String(quickPasteCat.DefaultForTypes) + "' " + "WHERE QuickPasteCatNum = " + POut.Long(quickPasteCat.QuickPasteCatNum);
        Db.NonQ(command);
    }

    /**
    * Updates one QuickPasteCat in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(QuickPasteCat quickPasteCat, QuickPasteCat oldQuickPasteCat) throws Exception {
        String command = "";
        if (quickPasteCat.Description != oldQuickPasteCat.Description)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "Description = '" + POut.String(quickPasteCat.Description) + "'";
        }
         
        if (quickPasteCat.ItemOrder != oldQuickPasteCat.ItemOrder)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "ItemOrder = " + POut.Int(quickPasteCat.ItemOrder) + "";
        }
         
        if (quickPasteCat.DefaultForTypes != oldQuickPasteCat.DefaultForTypes)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "DefaultForTypes = '" + POut.String(quickPasteCat.DefaultForTypes) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE quickpastecat SET " + command + " WHERE QuickPasteCatNum = " + POut.Long(quickPasteCat.QuickPasteCatNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one QuickPasteCat from the database.
    */
    public static void delete(long quickPasteCatNum) throws Exception {
        String command = "DELETE FROM quickpastecat " + "WHERE QuickPasteCatNum = " + POut.Long(quickPasteCatNum);
        Db.NonQ(command);
    }

}


