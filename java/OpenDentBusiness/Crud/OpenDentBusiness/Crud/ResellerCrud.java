//
// Translated by CS2J (http://www.cs2j.com): 2/15/2016 8:08:06 PM
//

package OpenDentBusiness.Crud;

import CS2JNet.System.StringSupport;

//This file is automatically generated.
//Do not attempt to make changes to this file because the changes will be erased and overwritten.
public class ResellerCrud   
{
    /**
    * Gets one Reseller object from the database using the primary key.  Returns null if not found.
    */
    public static Reseller selectOne(long resellerNum) throws Exception {
        String command = "SELECT * FROM reseller " + "WHERE ResellerNum = " + POut.Long(resellerNum);
        List<Reseller> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets one Reseller object from the database using a query.
    */
    public static Reseller selectOne(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Reseller> list = TableToList(Db.GetTable(command));
        if (list.Count == 0)
        {
            return null;
        }
         
        return list[0];
    }

    /**
    * Gets a list of Reseller objects from the database using a query.
    */
    public static List<Reseller> selectMany(String command) throws Exception {
        if (RemotingClient.RemotingRole == RemotingRole.ClientWeb)
        {
            throw new ApplicationException("Not allowed to send sql directly.  Rewrite the calling class to not use this query:\r\n" + command);
        }
         
        List<Reseller> list = TableToList(Db.GetTable(command));
        return list;
    }

    /**
    * Converts a DataTable to a list of objects.
    */
    public static List<Reseller> tableToList(DataTable table) throws Exception {
        List<Reseller> retVal = new List<Reseller>();
        Reseller reseller = new Reseller();
        for (int i = 0;i < table.Rows.Count;i++)
        {
            reseller = new Reseller();
            reseller.ResellerNum = PIn.Long(table.Rows[i]["ResellerNum"].ToString());
            reseller.PatNum = PIn.Long(table.Rows[i]["PatNum"].ToString());
            reseller.UserName = PIn.String(table.Rows[i]["UserName"].ToString());
            reseller.ResellerPassword = PIn.String(table.Rows[i]["ResellerPassword"].ToString());
            retVal.Add(reseller);
        }
        return retVal;
    }

    /**
    * Inserts one Reseller into the database.  Returns the new priKey.
    */
    public static long insert(Reseller reseller) throws Exception {
        if (DataConnection.DBtype == DatabaseType.Oracle)
        {
            reseller.ResellerNum = DbHelper.GetNextOracleKey("reseller", "ResellerNum");
            int loopcount = 0;
            while (loopcount < 100)
            {
                try
                {
                    return insert(reseller,true);
                }
                catch (Oracle.DataAccess.Client.OracleException ex)
                {
                    if (ex.Number == 1 && ex.Message.ToLower().Contains("unique constraint") && ex.Message.ToLower().Contains("violated"))
                    {
                        reseller.ResellerNum++;
                        loopcount++;
                    }
                    else
                    {
                        throw ex;
                    } 
                }
            
            }
            throw new ApplicationException("Insert failed.  Could not generate primary key.");
        }
        else
        {
            return insert(reseller,false);
        } 
    }

    /**
    * Inserts one Reseller into the database.  Provides option to use the existing priKey.
    */
    public static long insert(Reseller reseller, boolean useExistingPK) throws Exception {
        if (!useExistingPK && PrefC.RandomKeys)
        {
            reseller.ResellerNum = ReplicationServers.GetKey("reseller", "ResellerNum");
        }
         
        String command = "INSERT INTO reseller (";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += "ResellerNum,";
        }
         
        command += "PatNum,UserName,ResellerPassword) VALUES(";
        if (useExistingPK || PrefC.RandomKeys)
        {
            command += POut.Long(reseller.ResellerNum) + ",";
        }
         
        command += POut.Long(reseller.PatNum) + "," + "'" + POut.String(reseller.UserName) + "'," + "'" + POut.String(reseller.ResellerPassword) + "')";
        if (useExistingPK || PrefC.RandomKeys)
        {
            Db.NonQ(command);
        }
        else
        {
            reseller.ResellerNum = Db.NonQ(command, true);
        } 
        return reseller.ResellerNum;
    }

    /**
    * Updates one Reseller in the database.
    */
    public static void update(Reseller reseller) throws Exception {
        String command = "UPDATE reseller SET " + "PatNum          =  " + POut.Long(reseller.PatNum) + ", " + "UserName        = '" + POut.String(reseller.UserName) + "', " + "ResellerPassword= '" + POut.String(reseller.ResellerPassword) + "' " + "WHERE ResellerNum = " + POut.Long(reseller.ResellerNum);
        Db.NonQ(command);
    }

    /**
    * Updates one Reseller in the database.  Uses an old object to compare to, and only alters changed fields.  This prevents collisions and concurrency problems in heavily used tables.
    */
    public static void update(Reseller reseller, Reseller oldReseller) throws Exception {
        String command = "";
        if (reseller.PatNum != oldReseller.PatNum)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "PatNum = " + POut.Long(reseller.PatNum) + "";
        }
         
        if (reseller.UserName != oldReseller.UserName)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "UserName = '" + POut.String(reseller.UserName) + "'";
        }
         
        if (reseller.ResellerPassword != oldReseller.ResellerPassword)
        {
            if (!StringSupport.equals(command, ""))
            {
                command += ",";
            }
             
            command += "ResellerPassword = '" + POut.String(reseller.ResellerPassword) + "'";
        }
         
        if (StringSupport.equals(command, ""))
        {
            return ;
        }
         
        command = "UPDATE reseller SET " + command + " WHERE ResellerNum = " + POut.Long(reseller.ResellerNum);
        Db.NonQ(command);
    }

    /**
    * Deletes one Reseller from the database.
    */
    public static void delete(long resellerNum) throws Exception {
        String command = "DELETE FROM reseller " + "WHERE ResellerNum = " + POut.Long(resellerNum);
        Db.NonQ(command);
    }

}


